import { Highlightandstrikedir } from './highlightandstrikedir';

describe('Highlightandstrikedir', () => {
  it('should create an instance', () => {
    const directive = new Highlightandstrikedir();
    expect(directive).toBeTruthy();
  });
});
