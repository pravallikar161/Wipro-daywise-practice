import { CentitofahrenPipe } from './centitofahren-pipe';

describe('CentitofahrenPipe', () => {
  it('create an instance', () => {
    const pipe = new CentitofahrenPipe();
    expect(pipe).toBeTruthy();
  });
});
